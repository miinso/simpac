diff --git a/BUILD.bazel b/BUILD.bazel
index 3881d68..36e1913 100644
--- a/BUILD.bazel
+++ b/BUILD.bazel
@@ -37,11 +37,14 @@ cc_library(
     ]),
     copts = ["-w"] + select({
         "@platforms//os:windows": [""],
-        "//conditions:default": ["-mwaitpkg"],
+        "@platforms//os:emscripten": ["-fexceptions"],
+        "@platforms//cpu:x86_64": ["-mwaitpkg"],
+        "//conditions:default": [""],
     }),
     defines =
         select({
             "@platforms//cpu:x86_64": ["__TBB_NO_IMPLICIT_LINKAGE"],
+            "@platforms//os:emscripten": ["TBB_USE_EXCEPTIONS=0"],
             "//conditions:default": [
                 "USE_PTHREAD",
             ],
@@ -56,6 +59,7 @@ cc_library(
     linkopts =
         select({
             "@platforms//os:windows": [],
+            "@platforms//os:emscripten": ["-fexceptions"],
             "@platforms//os:linux": [
                 "-ldl",
                 "-pthread",
